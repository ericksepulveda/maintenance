<%= simple_form_for(@facility) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :community %>
    <%= f.input :days_to_check %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
